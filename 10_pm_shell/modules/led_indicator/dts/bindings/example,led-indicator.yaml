description: Example LED indicator driver

compatible: "example,led-indicator"

include: base.yaml

properties:
  status-gpios:
    type: phandle-array
    required: true
    description: GPIO pin for the LED controlled by this driver
